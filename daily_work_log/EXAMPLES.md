# 使用示例

## 快速开始

### 方法1：使用启动脚本（最简单）

```bash
./quick_start.sh
```

然后根据菜单选择操作模式。

### 方法2：直接使用命令

```bash
# 自动生成今日日志
python3 generate_log.py

# 交互式输入
python3 generate_log.py -i

# 快速记录
python3 generate_log.py --quick "完成了项目方案设计"
```

## 完整使用流程示例

### 场景1：第一次使用

```bash
# 1. 编辑项目数据，添加你的项目
vim data/projects.json

# 2. 编辑任务数据，添加待办事项
vim data/tasks.json

# 3. 运行交互式输入
python3 generate_log.py -i

# 4. 查看生成的日志
cat logs/$(date +%Y-%m)/$(date +%Y-%m-%d).md
```

### 场景2：每日工作结束时

```bash
# 运行交互式模式，记录今日工作
python3 generate_log.py -i
```

交互过程示例：
```
==============================================================
  办公日志生成工具 - 交互式输入
==============================================================

📋 今日待办事项：
  1. 🔴 完成MAC控制器测试用例编写
     项目: 以太网MAC控制器开发
  2. 🟡 更新项目文档
     项目: 以太网MAC控制器开发
  3. 🟡 准备周报

✅ 请选择今天完成的任务（输入序号，多个用逗号分隔，按回车跳过）：
完成的任务: 1,2

📝 其他完成的工作（每行一项，空行结束）：
  - 参加部门技术分享会
  - 协助新同事解决问题
  -

➕ 添加新的待办任务（可选，空行跳过）：
新任务: 优化代码性能

❓ 遇到的问题或需要注意的事项（可选，空行跳过）：
  - 测试环境配置需要升级
  -

📅 明日计划（每行一项，空行结束）：
  - 继续优化代码
  - 完成周报
  -

🗓️ 是否有会议需要记录？(y/n): y
会议时间（如 10:00-11:00）: 14:00-15:00
会议主题: 项目进度评审
参会人员: 张三、李四、王五
会议要点: 讨论了项目进度，决定下周进入测试阶段

✅ 日志生成成功！
📁 保存位置: logs/2025-11/2025-11-17.md
```

### 场景3：快速记录单个工作

```bash
# 完成一项工作后立即记录
python3 generate_log.py --quick "完成了数据库优化，查询性能提升30%"

# 多次快速记录会累加
python3 generate_log.py --quick "修复了登录模块的bug"
python3 generate_log.py --quick "更新了API文档"
```

### 场景4：生成历史日期的日志

```bash
# 补充昨天的日志
python3 generate_log.py --date 2025-11-16

# 批量生成上周的日志
for i in {10..16}; do
    python3 generate_log.py --date 2025-11-$i
done
```

## 项目管理示例

### 添加新项目

编辑 `data/projects.json`：

```json
{
  "projects": [
    {
      "id": "proj003",
      "name": "用户管理系统重构",
      "description": "重构现有用户管理系统，提升性能和可维护性",
      "progress": 0,
      "status": "即将开始",
      "start_date": "2025-11-20",
      "deadline": "2025-12-20",
      "priority": "high",
      "tasks": [
        {
          "name": "需求梳理",
          "status": "pending"
        },
        {
          "name": "架构设计",
          "status": "pending"
        }
      ]
    }
  ]
}
```

### 更新项目进度

当项目有进展时，更新对应的 `progress` 值和任务状态：

```json
{
  "progress": 35,  // 从0更新到35
  "tasks": [
    {
      "name": "需求梳理",
      "status": "completed",
      "completion_date": "2025-11-21"
    },
    {
      "name": "架构设计",
      "status": "in_progress",
      "progress": 50
    }
  ]
}
```

## 任务管理示例

### 添加待办任务

编辑 `data/tasks.json`：

```json
{
  "tasks": [
    {
      "id": "task004",
      "content": "准备技术分享PPT",
      "project_id": null,
      "priority": "medium",
      "status": "pending",
      "created_date": "2025-11-17",
      "due_date": "2025-11-19",
      "estimated_hours": 3
    }
  ]
}
```

### 标记任务完成

两种方式：

1. **交互式**：运行 `python3 generate_log.py -i` 时选择完成的任务
2. **手动**：编辑JSON文件，修改状态为 `completed` 并添加完成日期

## 自定义模板示例

### 创建简洁版模板

创建 `templates/simple_template.md`：

```markdown
# {date_cn} 工作日志

## 完成工作
{tasks}

## 明日计划
{next_plan}
```

然后修改 `config.yaml`：

```yaml
template_file: "./templates/simple_template.md"
```

### 创建详细版模板

创建 `templates/detailed_template.md`：

```markdown
# 工作日志 - {date_cn} {weekday}

## 📊 今日概要
{summary}

## 💼 项目进展
{work_content}

## ✅ 任务完成情况 ({completed_count}/{total_count})
{tasks}

{meetings_section}

{problems_section}

## 📈 数据统计
{statistics_section}

## 🎯 明日目标
{next_plan}

## 💡 心得体会
[手动填写]

---
生成时间：{generated_time}
```

## 导出和分享

### 导出为PDF

```bash
# 使用pandoc导出
pandoc logs/2025-11/2025-11-17.md -o daily_log_2025-11-17.pdf

# 批量导出本月所有日志
for file in logs/2025-11/*.md; do
    pandoc "$file" -o "${file%.md}.pdf"
done
```

### 生成周报

```bash
# 手动合并本周日志
cat logs/2025-11/2025-11-{13..17}.md > weekly_report_2025-w46.md
```

### 结合Git使用

```bash
# 每天自动提交日志
python3 generate_log.py -i
cd ..
git add daily_work_log/logs/
git commit -m "docs: $(date +%Y-%m-%d)工作日志"
```

## 高级技巧

### 1. 创建别名

在 `.bashrc` 或 `.zshrc` 中添加：

```bash
alias log-today='cd /path/to/daily_work_log && python3 generate_log.py -i'
alias log-quick='cd /path/to/daily_work_log && python3 generate_log.py --quick'
alias log-view='cat /path/to/daily_work_log/logs/$(date +%Y-%m)/$(date +%Y-%m-%d).md'
```

使用：
```bash
log-today          # 交互式生成今日日志
log-quick "工作内容"  # 快速记录
log-view           # 查看今日日志
```

### 2. 定时提醒

使用cron在每天下班前提醒：

```bash
# 编辑crontab
crontab -e

# 添加：每天18:00提醒填写日志
0 18 * * * notify-send "工作日志" "记得填写今日工作日志！"
```

### 3. 自动备份

创建备份脚本 `backup.sh`：

```bash
#!/bin/bash
DATE=$(date +%Y%m%d)
tar -czf backup_logs_$DATE.tar.gz logs/ data/
mv backup_logs_$DATE.tar.gz ~/backups/
```

### 4. 数据分析

统计本月工作量：

```bash
# 统计本月完成的任务数
grep -r "\[x\]" logs/2025-11/ | wc -l

# 统计本月参加的会议数
grep -r "## 会议记录" logs/2025-11/ | wc -l
```

## 常见问题排查

### Q: 运行报错 "No module named 'yaml'"

```bash
pip install pyyaml
```

### Q: 日志内容为空或不完整

检查数据文件是否正确：
```bash
# 验证JSON格式
python3 -m json.tool data/projects.json
python3 -m json.tool data/tasks.json
```

### Q: 如何重新生成日志

直接再次运行生成命令即可覆盖：
```bash
python3 generate_log.py --date 2025-11-17
```

## 更多示例

完整的示例代码和配置文件请参考：
- 项目数据示例：`data/projects.json`
- 任务数据示例：`data/tasks.json`
- 配置文件示例：`config.yaml`
- 默认模板：`templates/default_template.md`

---

祝你使用愉快！如有问题欢迎反馈。
